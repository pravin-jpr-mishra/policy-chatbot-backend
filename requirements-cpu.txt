# Core FastAPI and server
fastapi
uvicorn[standard]
python-multipart

# Authentication
msal

# Environment and utilities
python-dotenv
pydantic
requests
aiofiles

# AI and LLM
groq

# LangChain packages (ALL of them your app uses)
langchain
langchain-community
langchain-huggingface
langchain-chroma
langchain-core
langchain-text-splitters

# Vector store and embeddings (CPU only)
chromadb
sentence-transformers

# Text processing
pypdf2
python-docx

# Avoid heavy packages - use CPU-only versions
numpy
pandas

# If needed, add these CPU-only torch packages (but they're large)
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch --index-url https://download.pytorch.org/whl/cpu